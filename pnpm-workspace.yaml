packages:
  - "."

# Delay installs of brand-new package versions (minutes). 10080 = 7 days.
minimumReleaseAge: 10080

# Reject updates that weaken publish provenance / trust signals.
trustPolicy: no-downgrade
