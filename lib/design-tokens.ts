/**
 * FitTrack Routine Dashboard — design tokens
 * Palette / nav live in `app/globals.css` and layout nav lists.
 */

/** Hex for `theme-color` meta — keep in sync with `--background` / `.dark --background` in globals.css */
export const fitTrackThemeColor = {
  light: "#f7f6f4",
  dark: "#292725",
} as const;
